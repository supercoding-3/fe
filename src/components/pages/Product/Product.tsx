import './product.scss';
import { HeaderNavigation } from '@/components/layout';

const Product = () => {
  return (
    <section>
      <HeaderNavigation />
    </section>
  );
};

export default Product;
